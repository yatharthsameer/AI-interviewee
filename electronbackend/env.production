# PRODUCTION ENVIRONMENT VARIABLES
# Copy to .env and customize for your deployment

# === REQUIRED: Environment ===
NODE_ENV=production

# === REQUIRED: API Keys ===
GEMINI_API_KEY=your_production_gemini_key
CHATGPT_API_KEY=your_production_openai_key

# === REQUIRED: Security ===
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=generate_a_secure_32_character_hex_string

# === REQUIRED: Database ===
# For cloud deployment (PostgreSQL recommended)
DATABASE_URL=postgresql://username:password@hostname:5432/dbname
# Generate with: openssl rand -hex 16
DB_PASSWORD=generate_secure_db_password

# === REQUIRED: CORS ===
# Your Electron app and admin dashboard domains
ALLOWED_ORIGINS=https://your-electron-app-domain.com,https://admin.yourdomain.com

# === Optional: Hosting ===
HOST=0.0.0.0
PORT=5002

# === Optional: Monitoring ===
# SENTRY_DSN=your_sentry_dsn_for_error_tracking 